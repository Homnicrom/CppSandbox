# CMake
build/
out/

# Per-developer preset overrides; CMakePresets.json is the shared one and is committed
CMakeUserPresets.json

# clangd index cache
.cache/

# Visual Studio
.vs/
*.user
*.sdf
*.opensdf
*.suo
*.VC.db
*.VC.opendb

# MSBuild output (if native .sln/.vcxproj ever return)
[Dd]ebug/
[Rr]elease/
x64/
x86/
Win32/

# ASan drill binaries (g++ -o d1 next to the drill, -o drill1 at the root as in CI)
MemorySafetyDrills/d[0-9]
MemorySafetyDrills/d[0-9].exe
/drill[0-9]
/drill[0-9].exe
